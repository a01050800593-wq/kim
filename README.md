# kim

Streamlit으로 만드는 1페이지 MVP입니다. 순수 Python으로 구성하며 외부 DB는 사용하지 않습니다.

## 실행

```sh
pip install -r requirements.txt
streamlit run app.py
```

API 키나 기타 비밀값은 코드에 작성하지 말고 환경변수 또는 `.streamlit/secrets.toml`에 설정합니다.
